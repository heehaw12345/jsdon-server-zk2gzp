{
  "name": "node-starter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "frontend": "http-server",
    "backend": "json-server -w db.json"
  },
  "dependencies": {
    "http-server": "^0.13.0"
  },
  "devDependencies": {
    "json-server": "^0.16.3"
  }
}
